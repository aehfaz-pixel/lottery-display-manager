{
  "name": "lottery-electron",
  "version": "1.0.36",
  "description": "Lottery Manager — Electron App",
  "author": "Lottery Manager",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "set NODE_ENV=development && electron .",
    "rebuild-hid": "electron-rebuild -f -w uiohook-napi",
    "prerelease-check": "node pre-release-check.js",
    "build": "electron-builder --publish always && node fix-release.js",
    "release": "node pre-release-check.js && npm version patch --no-git-tag-version && node sync-version.js && electron-builder --publish always && node fix-release.js"
  },
  "dependencies": {
    "electron-updater": "^6.3.9",
    "express": "^5.2.1",
    "uiohook-napi": "^1.5.5"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0",
    "electron-rebuild": "^3.2.9"
  },
  "build": {
    "appId": "com.lottery.manager",
    "productName": "Lottery-Manager",
    "files": [
      "main.js",
      "preload.js",
      "server.js",
      "src/**/*",
      "node_modules/**/*"
    ],
    "win": {
      "target": "nsis"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    },
    "publish": {
      "provider": "github",
      "owner": "aehfaz-pixel",
      "repo": "lottery-display-manager",
      "private": false,
      "releaseType": "release"
    }
  }
}
